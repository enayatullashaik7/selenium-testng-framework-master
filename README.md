Built an UI Test Automation Framework based on below problem statement using Page object model framework (TestNg).

Problem Statement (Build a UI testing framework to test for UI elements validation when a user traverses FR credit card Applications
